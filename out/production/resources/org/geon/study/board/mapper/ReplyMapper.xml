<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.geon.study.board.mapper.ReplyMapper">

    <select id="getReply" resultType="org.geon.study.board.dto.Reply">
        SELECT * FROM TB_REPLY WHERE BNO = #{bno} ORDER BY RNO DESC
    </select>

    <insert id="insertReply">
        INSERT INTO TB_REPLY (BNO, NICKNAME, REPLY) VALUES (#{bno}, #{nickname}, #{reply})
    </insert>
</mapper>